#include <stdio.h>

int main()
{
    int n, a, d, sum;
    scanf("%d", &n);
    a = -5, d = (1- (-5));
    
    sum = n * (2 * a + (n - 1) * d) / 2;
    
    printf("%d\n", sum);
    
    return 0;
}
